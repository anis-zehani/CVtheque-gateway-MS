spring:
  application:
    name: gateway-MS
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-MS
      fail-fast: true
   
management:
  endpoints:
    web:
      exposure:
        include: ["health", "info", "refresh"]